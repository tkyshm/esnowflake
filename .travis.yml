language: erlang
otp_release:
  - 19.0
  - 20.0
  - 20.1.3

cache:
  directories:
    - _build

before_script:
  - git clone https://github.com/inaka/elvis
  - cd elvis
  - rebar3 escriptize
  - cp _build/default/bin/elvis /usr/local/bin
  - cd -
  - rebar3 compile

script: make test
